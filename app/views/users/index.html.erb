<div class="row">
  <% @users.each do |user| %>
  
  <% if false %>
    <table class="table table-bordered">
        <td class="col-xs-offset-5 text-center">
          <span class="gravatar">
            <img src="<%= gravatar_url(user, size: 20) %>" alt="" class="img-circle">
            <%= link_to user.name, user %>
          </span>
        </td>
      </tr>
    </table>
  <% end %>
    
    <!--<table class="table table-bordered">-->
        <!--<td class="col-xs-offset-5 text-center">-->
        <div class="col-xs-3 text-center" style="border: 1px solid #ccc; height: 50px; line-height: 50px;">
          <span class="gravatar">
            <img src="<%= gravatar_url(user, size: 20) %>" alt="" class="img-circle">
            <%= link_to user.name, user %>
          </span>
        </div>
      <!--</tr>-->
    <!--</table>-->
  <% end %>
</div>